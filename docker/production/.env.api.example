# Application
APP_NAME=<APP_NAME>                            # Name of your application
PORT=<PORT>                                    # Port the application will listen on
TITLE=<TITLE>                                  # Title of the API
DESCRIPTION=<DESCRIPTION>                      # Description of the API
VERSION=<VERSION>                              # Version of the API
AUTHOR=<AUTHOR>                                # Author(s) of the API

# MongoDB Credentials
DATA_CONNECTION_METHOD=<CONNECTION_METHOD>     # mongodb or mongodb+srv
DATA_SOURCE_USERNAME=<USERNAME>                # MongoDB username
DATA_SOURCE_PASSWORD=<PASSWORD>                # MongoDB password
DATA_SOURCE_DOMAIN=<DOMAIN>                    # MongoDB host/domain
DATA_SOURCE_DB=<DATABASE>                      # Database name
DATA_PARAMS=<CONNECTION_PARAMS>                # Connection parameters (e.g., retryWrites=true)

# Redis Credentials
CACHE_TYPE=<CACHE_TYPE>                        # Cache type (redis)
CACHE_HOST=<REDIS_HOST>                        # Redis host
CACHE_PORT=<REDIS_PORT>                        # Redis port
CACHE_DB=<REDIS_DB>                            # Redis database index
CACHE_USERNAME=<REDIS_USERNAME>                # Redis username
CACHE_PASSWORD=<REDIS_PASSWORD>                # Redis password
CACHE_TIMEOUT=<CACHE_TIMEOUT>                  # Timeout for cache operations (ms)
CACHE_LETTUCE_POOL_MAX_ACTIVE=<MAX_ACTIVE>     # Max active connections
CACHE_LETTUCE_POOL_MAX_WAIT=<MAX_WAIT>         # Max wait time for connections
CACHE_LETTUCE_POOL_MAX_IDLE=<MAX_IDLE>         # Max idle connections
CACHE_LETTUCE_POOL_MIN_IDLE=<MIN_IDLE>         # Min idle connections

# Cache Configuration
CACHE_TIME_TO_LIVE=<CACHE_TTL>                 # Cache TTL (ms)
CACHE_NULL_VALUES=<BOOLEAN>                    # Store null values (true/false)

# Email Credentials
MAIL_HOST=<SMTP_HOST>                          # SMTP server
MAIL_PORT=<SMTP_PORT>                          # SMTP port (e.g., 587)
MAIL_USERNAME=<SMTP_USERNAME>                  # SMTP login username
MAIL_PASSWORD=<SMTP_PASSWORD>                  # SMTP login password
MAIL_PROPERTIES_SMTP_AUTH=<TRUE/FALSE>         # Enable SMTP authentication
MAIL_PROPERTIES_SMTP_STARTTLS_ENABLE=<TRUE/FALSE>  # Enable STARTTLS

# JWT Credentials
SECURITY_JWT_SECRET_KEY=<JWT_SECRET_KEY>       # Secret key for signing JWT tokens
SECURITY_JWT_EXPIRATION=<JWT_EXPIRATION>       # JWT expiration time (ms)
SECURITY_PUBLIC_ROUTES=<PUBLIC_ROUTES>         # Public routes that don't require auth
SECURITY_AES_KEY=<AES_KEY>                     # AES encryption key
SECURITY_SCHEME_NAME=<SECURITY_SCHEME_NAME>    # Name of the security scheme

# Rate Limiting
RATE_LIMITING_MAX_REQUESTS=<MAX_REQUESTS>      # Max requests per window
RATE_LIMITING_TIME_WINDOW=<TIME_WINDOW>        # Time window (ms)
RATE_LIMITING_PUBLIC_ROUTES=<PUBLIC_ROUTES>    # Public routes excluded from rate limiting

# HTTPS Headers (CORS)
HEADER_CORS_ALLOWED_ORIGINS=<ALLOWED_ORIGINS>  # Allowed origins (comma-separated)

# Tomcat Configuration
SERVER_TOMCAT_TIMEOUT=<TOMCAT_TIMEOUT>         # Timeout (ms)

# Logging
DEBUGGER_MODE=<DEBUG_MODE>                     # Log level (INFO, DEBUG, OFF)